# Agent notes

This project’s lasting knowledge lives in `.cursor/memory/`.
Read `overview.md` first, then only the memory file that matches the task.

- Flutter/Dart app entry point: `lib/main.dart`.
- Verify changes with `flutter analyze` and `flutter test`.
- Keep `pubspec.yaml`, `lib/main.dart`, and `CHANGELOG.md` versioned together.
