# Task Handoff

- Task ID: T01
- STATUS: DONE
- Summary: Candle draw (body then wicks, green/red by drag) and six default pattern stamps are in `lib/main.dart`, with `slots_migrated_v4` and `kAppVersion` 1.4.0.

## CHANGED FILES

- lib/main.dart
- .agent/execution-plan.md
- .agent/dependency-graph.md
- .agent/execution-state.md
- .agent/handoffs/T01.md

## DECISIONS

- `ToolType.candlestick` (draw) and `ToolType.candlePattern` (stamp). Pattern id stored on the slot as `candlePattern` string (same idea as `presetText`).
- Drawable JSON `kind: 'candles'` via `CandleGroupDrawable` + `CandleSpec` (x, width, openY, closeY, highY, lowY). Body colors are always `kCandleBullColor` / `kCandleBearColor`; slot color is toolbar tint only.
- Two-step draw: first pan becomes the body (`candleSpecFromBodyDrag`); `_pendingCandle` waits for the wick pan (`applyCandleWicks`). Click, Escape, Pointer, or another tool commits default wicks (`_commitPendingCandle`).
- Default slots after rectangle: `candle-draw`, `candle-doji`, `candle-hammer`, `candle-bull-engulf`, `candle-bear-engulf`, `candle-morning-star`, `candle-evening-star`. Shooting Star is Add-slot only.
- One-time `slots_migrated_v4`: insert those seven after the last rectangle (or at end). Flag also set on fresh/default loads so a restart does not duplicate.

## TESTS

- Command/check: `flutter analyze lib/main.dart`
- Result: No issues found (exit 0). Full `flutter test` is T02.

## KNOWN ISSUES

- None in app behavior.
- `pubspec.yaml` / CHANGELOG / README are still pre-1.4.0; T02 owns them.
- Saved-slot tests that set `slots_migrated_v3` but not `slots_migrated_v4` will gain the seven candle tools on load (same pattern as v3).

## OUTPUTS

- Working Candle tool and pattern stamps; persist + v4 migration; version 1.4.0 in `kAppVersion`.

## NEXT-AGENT NOTES

- T02 owns `test/widget_test.dart`, `CHANGELOG.md`, `README.md`, `pubspec.yaml` only. Do not edit `lib/main.dart`.
- Keys: `tool-candle-draw`, `tool-candle-doji`, `tool-candle-hammer`, `tool-candle-bull-engulf`, `tool-candle-bear-engulf`, `tool-candle-morning-star`, `tool-candle-evening-star`. Pattern tap must not open `inline-text-field`.
- Helpers for pure tests: `candleDragIsBullish`, `candleSpecFromBodyDrag`, `applyCandleWicks`, `candlePatternSpecs`, `CandlePattern.candleCount` (1 / 2 / 3), `CandleSpec.isBullish`, `drawableFromJson` / `toJson` with `kind: 'candles'`, `ToolSlot.candlePattern`.
- Migration flag: `slots_migrated_v4`. Empty prefs already include the new defaults and should set the flag.
- Suggested tests from the plan: bullish vs bearish from drag direction; stamp candle counts; JSON round-trip of a group and of `candlePattern` on a slot; empty prefs show `tool-candle-draw` plus Doji and Morning Star; select Doji, tap canvas → one `CandleGroupDrawable` with one candle; saved slots without `slots_migrated_v4` get the new tools once.
- Version line uses `kAppVersion` (`1.4.0`). Pubspec should become `1.4.0+5`.
